'use client'

import { GameState } from '@settlers/core'

interface InteractionLayerProps {
  gameState: GameState
}

export function InteractionLayer({ }: InteractionLayerProps) {
  // TODO: Add tooltips, highlights, and other interactive overlays
  return null
} 